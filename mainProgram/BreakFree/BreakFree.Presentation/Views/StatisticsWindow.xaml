<Window x:Class="BreakFree.Presentation.Views.StatisticsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:BreakFree.Presentation.Views"
        Title="BreakFree - Статистика"
        Height="600"
        Width="450"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow">

    <Window.Resources>

        <!-- Стилі кнопок -->
        <Style TargetType="Button"
               x:Key="PrimaryButton">
            <Setter Property="Background"
                    Value="#616161"/>
            <Setter Property="Foreground"
                    Value="White"/>
            <Setter Property="FontSize"
                    Value="14"/>
            <Setter Property="Padding"
                    Value="10,5"/>
            <Setter Property="BorderThickness"
                    Value="0"/>
            <Setter Property="FontWeight"
                    Value="Bold"/>
        </Style>

        <Style TargetType="Button"
               x:Key="SecondaryButton">
            <Setter Property="Background"
                    Value="#9E9E9E"/>
            <Setter Property="Foreground"
                    Value="White"/>
            <Setter Property="FontSize"
                    Value="14"/>
            <Setter Property="Padding"
                    Value="10,5"/>
            <Setter Property="BorderThickness"
                    Value="0"/>
            <Setter Property="FontWeight"
                    Value="Bold"/>
        </Style>

        <Style x:Key="AchievementTextDisabled"
               TargetType="TextBlock">
            <Setter Property="Foreground"
                    Value="#9E9E9E"/>
            <!-- світло-сірий -->
        </Style>

        <Style x:Key="AchievementTextEnabled"
               TargetType="TextBlock">
            <Setter Property="Foreground"
                    Value="#333333"/>
            <!-- темний нормальний текст -->
        </Style>


        <!-- Конвертери -->
        <local:ZeroToVisibilityConverter x:Key="ZeroToVisibilityConverter"/>
        <local:BoolToAchievementColorConverter x:Key="BoolToAchievementColorConverter"/>

    </Window.Resources>


    <Grid Margin="20">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0"
                   Text="Статистика"
                   FontSize="22"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,20"
                   Foreground="#333"/>


        <!-- 1: Графік + тригери -->
        <Grid Grid.Row="1"
              Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Графік -->
            <Border Grid.Column="0"
                    Background="White"
                    BorderBrush="LightGray"
                    BorderThickness="1"
                    CornerRadius="5"
                    Margin="0,0,10,0"
                    Padding="5">
                <Canvas x:Name="ChartCanvas"
                        Background="#FAFAFA"
                        Margin="5,21,5,92"/>
            </Border>

            <!-- Топ-3 тригери -->
            <StackPanel Grid.Column="1"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center">

                <TextBlock Text="Топ-3 тригери:"
                           FontSize="16"
                           FontWeight="SemiBold"
                           Margin="0,0,0,10"/>

                <ItemsControl ItemsSource="{Binding TopTriggers}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0"
                                           Text="•"
                                           FontSize="16"
                                           FontWeight="Bold"
                                           Margin="0,0,8,0"/>

                                <TextBlock Grid.Column="1"
                                           Text="{Binding Trigger}"
                                           VerticalAlignment="Center"
                                           Foreground="#333"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <TextBlock Text="Немає даних"
                           Visibility="{Binding TopTriggers.Count,
                                                Converter={StaticResource ZeroToVisibilityConverter}}"
                           Foreground="Gray"
                           FontStyle="Italic"
                           HorizontalAlignment="Center"
                           Margin="0,10,0,0"/>
            </StackPanel>
        </Grid>


        <!-- 2: Досягнення -->
        <StackPanel Grid.Row="2"
                    Margin="0,0,0,20">

            <TextBlock Text="Досягнення:"
                       FontSize="16"
                       FontWeight="SemiBold"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,10"/>

            <ItemsControl ItemsSource="{Binding Achievements}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Horizontal"
                                    HorizontalAlignment="Center"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>

                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Width="80"
                                Height="80"
                                CornerRadius="40"
                                BorderBrush="LightGray"
                                BorderThickness="1"
                                Background="{Binding IsUnlocked, Converter={StaticResource BoolToAchievementColorConverter}}"
                                Margin="15,10">
                            <TextBlock Text="{Binding Title}"
                                       FontSize="14"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Center"
                                       HorizontalAlignment="Center">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <!-- За замовчуванням: НЕ розблоковано -->
                                        <Setter Property="Foreground"
                                                Value="#9E9E9E"/>

                                        <Style.Triggers>
                                            <!-- Якщо IsUnlocked = true -->
                                            <DataTrigger Binding="{Binding IsUnlocked}"
                                                    Value="True">
                                                <Setter Property="Foreground"
                                                        Value="#333333"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>

                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </StackPanel>


        <!-- 3: Кнопки -->
        <Grid Grid.Row="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0"
                    Content="Експорт"
                    Style="{StaticResource PrimaryButton}"
                    Height="40"
                    Margin="5"
                    Click="ExportButton_Click"/>

            <Button Grid.Column="1"
                    Content="Скасувати"
                    Style="{StaticResource SecondaryButton}"
                    Height="40"
                    Margin="5"
                    Click="CancelButton_Click"/>
        </Grid>

    </Grid>

</Window>
